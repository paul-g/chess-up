#include "Knight.hpp"

#include "Board.hpp"

using namespace std;

std::vector<std::pair<int, int> > Knight::validMoves() {
  std::vector<std::pair<int, int> > pos;
  cout << pos.size() << endl;
  return pos;
}
